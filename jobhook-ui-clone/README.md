# JobHook 



## What I included
- `index.html` — single-page UI clone using Poppins & Inter fonts via Google Fonts.
- `css/styles.css` — styling tuned for dark theme, yellow accent, rounded cards, and layout similar to screenshots.
- `js/main.js` — tiny interactions (search alert).
- `assets/hero-illustration.svg` and `assets/person-ill.svg` — SVG placeholders that approximate the hero and person illustrations.
- No external copyrighted images included. Replace the SVGs in `assets/` with original images if you have them.

## How to run
1. Unzip `jobhook-ui-clone.zip`.
2. Open `index.html` in your browser.
3. For best font match, an internet connection is required to load Google Fonts.

## Next steps (optional)
- Replace SVG placeholders with real PNG/SVG illustrations from the original site.
- Add animations and hover effects to match the original exactly.
- Convert to React/Next.js if you want componentized structure.

-- Generated by ChatGPT (UI clone for reference)
